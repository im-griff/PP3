//Appearance and collision mask setup
if (argument_count > 0)
    {
        sprite_index = argument[0]; //Set argument to sprite name [spr_bear,]
};
else
    {
        sprite_index = spr_bear;
};
image_index = 0;
image_speed = 0;

//Dog states
isBarking = false;

